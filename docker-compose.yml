version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: rc_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: repair_center
      MYSQL_USER: appuser
      MYSQL_PASSWORD: appsecret
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    build: ./backend
    container_name: rc_backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    depends_on:
      - mysql
    ports:
      - "8080:8080"
    volumes:
      - ./backend/src:/app/src:ro

  frontend:
    build: ./frontend
    container_name: rc_frontend
    restart: unless-stopped
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend/src:/usr/src/app/src:ro

volumes:
  mysql_data:
